# Optiskin Docker Compose Configuration
#
# IMPORTANT: Create a .env file with your secrets before running!
# Copy .env.example to .env and fill in secure values.
# Never commit .env to git.

services:
  db:
    image: postgres:16-alpine
    container_name: salonapp-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: salonapp
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required in .env}
      POSTGRES_DB: salonapp
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U salonapp"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: salonapp-backend
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://salonapp:${DB_PASSWORD}@db:5432/salonapp
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required in .env}
      DEBUG: "false"
      CORS_ORIGINS: '["http://localhost:3001", "http://localhost", "https://${DOMAIN:-localhost}"]'
      PHOTOS_PATH: /app/data/photos
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?ADMIN_PASSWORD is required in .env}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-60}
      RATE_LIMIT_LOGIN_PER_MINUTE: ${RATE_LIMIT_LOGIN_PER_MINUTE:-5}
    volumes:
      - photos_data:/app/data/photos
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8420/health')\""]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend-v2
      dockerfile: Dockerfile
    container_name: salonapp-frontend
    restart: unless-stopped
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    container_name: salonapp-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend

volumes:
  postgres_data:
  photos_data:
